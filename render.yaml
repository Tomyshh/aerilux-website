services:
  - type: web
    name: aerilux-website
    rootDir: .
    buildCommand: npm install --legacy-peer-deps && npm run build
    startCommand: npx serve -s build -l 10000
    envVars:
      - key: NODE_VERSION
        value: 22.14.0

